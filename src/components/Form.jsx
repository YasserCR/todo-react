const Form = () => {
    return (
        <form>
            <input placeholder="Capture la tarea a ejecutar" />
            <button>Agregar tarea</button>
        </form>
    )
};

export default Form;